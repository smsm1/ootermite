#!/usr/bin/bash -x
echo "Building..."
. ./solar.env
cd $SRC_ROOT/instsetoo_native
$PERL $SOLARENV/bin/build.pl --all -P4 -- -P2
RETVAL=$?

if [ $? -eq 0 ]; then
  cd $SRC_ROOT/instset_native
  $PERL $SOLARENV/bin/build.pl --all -P4 -- -P2
  RETVAL=$?
fi

exit $RETVAL
